# Tomcat configs
server:
  port: 8761

# Logger configs
logging:
  level:
    com:
      netflix:
        eureka: ERROR
        discovery: ERROR

# Eureka configs
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false